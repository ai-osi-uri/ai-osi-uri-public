# OSI Finance — 経理（請求AR・支払AP）自動化プロダクト

日本の中小企業向けに、**請求（AR）と支払い（AP）まわりの経理事務を Cowork から回す**ための
プロダクトです。契約取込から請求書発行、受領請求書の支払起票、会計SaaS（v1=マネーフォワード）との
突合、月次クローズまでをカバーし、**台帳テンプレ同梱・セットアップ自動化・顧客向け導入ガイド**つきで
非開発者でも導入できます（1 Cowork = 1 組織）。

> **設計の前提**：会計仕訳の正本は**会計SaaS（v1=マネーフォワード）**。支払(AP)は口座連携明細（銀行・カード）、
> 売上(AR)は `osi-finance-ar-sync` の売上計上・入金消込で MF に載せ、**MF を請求(AR)・支払(AP) 双方の単一会計正本**にする
> （v0.6.0 で売上=0 の片肺記帳を是正）。支払管理台帳・請求管理台帳は**資金管理（誰にいくらいつ・請求予定・採番・入金予定）の正本**。
> 二重計上を防ぐため、**カード払い（自動課金SaaS）は支払管理台帳に起票しない**（会計のカード連携が自動仕訳）。
> 入金の銀行連携が自動仕訳する場合は連携側を「売掛金消込」に振り替え、AR の消込を二重計上しない。

> **安全原則**：**送金しない／台帳を自動確定しない**（人レビュー必須）。AI は「準備」までを担当し、
> 送金・確定は人が行う。機微値（社名・登録番号・口座番号・実在支払先名）は `osi-finance-settings.md` に外出し。

## 導入（顧客・伴走先向け）

非開発者向けの手引きは **[`docs/導入ガイド.md`](docs/導入ガイド.md)** を参照。
最短ルートは、Cowork に次のように話しかけるだけです。

> 「**OSI Finance を導入して**」 / 「**経理を初期セットアップして**」

→ `osi-finance-setup` が対話で「設定生成 → Drive フォルダ作成 → 台帳配置 → コネクタ確認 →
スケジュール登録案内 → スモークテスト」まで進めます。

## スキル一覧

| スキル | 役割 | 区分 |
|---|---|---|
| `osi-finance-setup` | 初回セットアップ（設定生成・フォルダ・台帳配置・コネクタ確認・スケジュール案内・スモークテスト） | **導入（新規）** |
| `osi-finance-contract-intake` | 契約PDF収集 → 契約マスタ起票 → 月次請求スケジュール展開 | AR 起点 |
| `osi-finance-invoice` | 月次の請求書PDF発行 → Gmail下書き作成 → 台帳更新 | AR 発行 |
| `osi-finance-payment-intake` | 受領請求書（振込のみ）→ 支払予定起票（人レビュー）→ 振込情報整形 | AP 起票 |
| `osi-finance-payment-detect` | 日次で受領請求書の取りこぼしを検出 → 起票案を報告 | AP 検出 |
| `osi-finance-mf-sync` | 支払管理台帳の支払済 ⇔ 会計SaaS仕訳を突合 → 計上漏れ検出 | 突合（AP） |
| `osi-finance-ar-sync` | 請求台帳の請求済→売上計上((借)売掛金/(貸)売上+仮受消費税)・入金済→消込((借)普通預金/(貸)売掛金)を MF と請求書IDで突合 → 計上漏れ検出・人レビューのうえ登録 | 突合（AR） |
| `osi-finance-monthly` | 月次クローズのオーケストレーション（取込→仕訳→AP突合→AR突合→検算→報告） | 月次 |
| `osi-finance-dashboard` | 請求(AR)・支払(AP)・経費を1枚で見るライブ・アーティファクト生成（MF試算表PL/BSを正本・表示専用） | 可視化 |

## 台帳テンプレ（同梱）

`assets/templates/` に2ファイルを同梱。導入時に `osi-finance-setup` が Drive の規定ツリー
（`assets/schema/data-layout.yaml` 正本）に **Google スプレッドシート形式へ変換して配置**します
（xlsx は搬送用。運用正本はスプレッドシート）。

- `請求管理台帳_テンプレート.xlsx`：発行者設定／契約マスタ／月次請求スケジュール（記入例つき）
- `支払管理台帳_テンプレート.xlsx`：支払先マスタ（既定支払方法で振込/カードを振り分け）／月次支払管理／経費一覧（記入例つき）

> **証憑 PDF など大容量ファイルの Drive 格納はローカル同期フォルダにコピーする方式を推奨**
> （コネクタ直アップロードは大容量で不安定）。

## 組織固有値の外出し（配布向け抽象化）

社名・登録番号・振込先・税率・採番ルール・Drive ルート／フォルダ名・台帳ファイル名・支払先→科目マッピング
などの**組織固有値は、各スキルに直書きせず `osi-finance-settings` に集約**しています。

- 配布テンプレート：`skills/config/osi-finance-settings.example.md`（**プレースホルダのみ**。機微値は含まない）。
- 各組織は同ディレクトリに `osi-finance-settings.md` をコピーし、`{{ }}` を実値で埋めて使う
  （`osi-finance-setup` が対話生成します）。
- **実値版 `osi-finance-settings.md` はコミットしない**（`.gitignore` の `**/osi-finance-settings.md` で除外）。
- 各 osi-finance スキルの SKILL.md 冒頭に「組織固有値は osi-finance-settings を参照」の共通注記。
- スキル本文のプロバイダ名は**役割名＋例示**に統一（振込口座〔例: Trunk〕／法人カード〔例: UPSIDER〕）。
  会計SaaS は v1=マネーフォワード固定。

## 必要コネクタ

| 用途 | サービス | 必須/任意 |
|---|---|---|
| 会計SaaS | マネーフォワード クラウド会計（v1固定） | 必須 |
| ストレージ | Google Drive（＋ローカル同期） | 必須 |
| メール | Gmail または **Superhuman** | 必須 |
| 電子契約 | DocuSign（自社送付契約のメタ補完） | 任意 |

> **メール添付の取得には Superhuman が必要**（標準 Gmail コネクタでは添付取得不可）。

## セットアップ手順（手動で行う場合）

通常は `osi-finance-setup` に任せますが、手動で行う場合は次の通り。

1. `skills/config/osi-finance-settings.example.md` を `skills/config/osi-finance-settings.md` にコピー。
2. プレースホルダ `{{ }}` を自社の実値で埋める（発行者・振込先・税率・採番・Drive・台帳・支払先マッピング）。
3. 必要コネクタを接続。
4. Drive にフォルダ構成を作り、`assets/templates/` の台帳テンプレを配置（ローカル同期推奨）。
5. 既存台帳の列が `osi-finance-mf-sync/references/ledger-schema.md` に準拠しているか確認。
6. 日次・月次のスケジュールタスクを登録（`docs/導入ガイド.md` 参照）。

## バージョン

- v0.18.0：**内部仕訳帳（仕訳台帳）とローカル仕訳生成 — 経理コンソール化の第1弾（データレイアウト v4）**。
  新スプレッドシート**「仕訳台帳」**（仕訳帳／勘定科目マスタ〔標準28科目同梱〕／月次サマリ〔数式集計〕）を
  正本化し、**`osi-finance-journal`** を新設：台帳から AR請求（売掛/売上）・AR入金（消込）・AP支払・経費の
  4種の仕訳案をローカル生成 → 人レビュー → 状態=確定で記帳。参照ID×出所の重複防止・借貸一致検算・
  未仕訳検出つき。ACCOUNTING_SYNC=none なら**仕訳帳が会計の正本**（会計SaaSなしで経理が成立）、
  mf/freee ならエクスポート下書き＋突合の物差し。settings に LEDGER_JOURNAL_FILE / LEDGER_JOURNAL_SHEET_ID を
  新設。コンソール v0.18: **経費タブ**（経費一覧の表示＋📎経費取込プロンプト）と**仕訳タブ**
  （仕訳帳ビュー・月フィルタ・仕訳生成プロンプト）を追加、支払タブに📎請求書取込ボタン、ヘッダに再読込。
  全体設計は docs/keiri-console-grand-design.md（v0.19=月次クローズ・利益確認、v0.20=エクスポート整備）。

- v0.17.0：**操作者トラッキング（データレイアウト v3）＋ freee・銀行明細の CSV 突合**。
  行を持つ全タブに**作成者/作成日時/更新者/更新日時**列（雛形マスタは登録者）を標準装備：
  台帳への全書き込みで「誰がやったか」を **Cowork ログインユーザーのメールアドレス**で記録
  （新規行=作成者・以後不変／更新=更新者・毎回上書き。正本は `data-layout.yaml` の `operator_tracking`。
  契約書を誰が作成したかも契約マスタの作成者列が正本）。settings に **OPERATOR_EMAIL**（定時ジョブ等の
  フォールバック）と **AUDIT_LOG**（変更前後つき操作ログタブ・既定 off）を新設（§0-3）。
  ヘルスチェックを v3 で緩和：**ヘッダ行の自動検出（1行目固定をやめる）＋必須列の存在チェック（順序不問）**、
  未知の独自列は報告のみ。列はヘッダ名で解決（列番号決め打ち禁止）。
  **`04.連携データ/`**（freee/・銀行/）を新設し、**`osi-finance-freee-sync`**（freee 仕訳CSVとの突合・
  freee への書き込みなし）と **`osi-finance-bank-recon`**（銀行明細CSVによる入金・支払の裏取り突合、
  入金済/支払済への遷移は人のボタンのみ）を新設。コンソール v0.17 と対応
  （設定タブの操作者メール・freee/銀行トグルは CSV フォルダの存在を疎通確認）。

- v0.15.0：**コンソール対応の土台（データレイアウト v2＋契約書作成・雛形管理）**。
  `data-layout.yaml` を v2 に改訂：契約マスタの「方向(受注/発注)」「出所(手動/Gmail取込)」を必須化、
  「雛形ID」「envelope_id」列を追加、**ステータスから「終了」を廃止**（期間終了は契約終了日から導出）。
  請求ステータスを `未請求/下書き作成済/請求済/入金済` に改訂（差し戻しは人の操作のみ）、請求日を必須化。
  ※ 2026-08: 実台帳に合わせ **`下書き済`** に一本化（`下書き作成済` は実データに存在しなかった）。正規語彙は `assets/schema/data-layout.yaml`（layout_version 5）を参照。
  月次支払スケジュールを必須化（発注契約から自動導出が標準動作）。請求・支払に**突合結果/突合日時**列を
  新設（mf-sync/ar-sync 等が書き戻し、コンソールは表示のみ）。**「雛形マスタ」タブと `00.契約書/雛形/`** を新設。
  **`osi-finance-contract-draft` を新設**：①雛形から契約書ドラフト生成→Drive保存→契約マスタ起票
  ②雛形のAI生成登録 ③実契約の{{変数}}化による雛形登録（機微値スキャン必須）。
  `osi-finance-invoice` に**単票モード**（INV指定で1件だけ発行）を追加し、台帳更新を「下書き済」までに
  変更（「請求済」への遷移は人）。settings に **INTEGRATIONS**（GMAIL_INTAKE / ACCOUNTING_SYNC(mf|freee|none) /
  BANK_RECON / ESIGN）を新設 — 外部サービスをすべてオプション化し、コンソールのトグルと共有する。

- v0.14.0：**運用モード（参照専用／編集可）と顧客向け導入手順書**。
  `osi-finance-settings` に **`OPERATION_MODE`**（§0）を新設：**編集可**（既定・従来動作）／**参照専用**
  （読み取りと報告のみ。台帳・MF・メール下書きへの一切の書き込みをしない。起票「案」の画面提示までは可）。
  全スキルが settings 読込時にモード定義を確認する挙動レイヤーに加え、**権限側の強制**（参照専用時は
  Drive の SA 共有を閲覧者に・MF API を read スコープに）を setup／connect の手順に組み込んだ二重防御。
  `osi-finance-setup` ステップ1に運用モードの収集（項目0）、ステップ2の共有案内にモード別の権限マッピングを追加。
  **`docs/導入手順書.md`** を新設（顧客・管理者向け3部構成：①会社の準備＝組織プラグインZIP登録、
  ②担当者PC準備＝拡張導入とコネクタ接続、③初期設定＝「OSI Finance を導入して」）。

- v0.13.0：**データレイアウト仕様の正本化（パッケージ配布に向けた抽象化）**。
  `assets/schema/data-layout.yaml` を新設し、フォルダ構成と台帳2冊の全タブ・全列を機械可読で一本化
  （ledger-schema.md の「別紙」依存を解消、AR 側の連携列も明文化）。フォルダ構成を**ルート1点方式**に変更：
  顧客が決めるのは「OSI Finance ルートをどこに置くか」1問だけ（**共有ドライブ推奨**）、配下は規定ツリーで固定
  （既存フォルダ体系の組織は settings で従来どおり上書き可）。共有はルート1点（経理担当=編集／SA=編集／
  閲覧者=任意、**組織全体共有はしない**）。台帳の運用形式を**ネイティブ Google スプレッドシートに統一**
  （xlsx テンプレは搬送用、setup が変換配置しスプレッドシートIDを settings に記録。同時編集・API読み書き・
  大容量 xlsx の不安定さを解消）。**台帳ヘルスチェック**を setup スモークと monthly の Step 0 に追加
  （フォルダ・タブ・ヘッダ列を data-layout.yaml と照合。差分は自動修復せず一覧報告）。

- v0.12.0：**支払漏れ防止（事故からの反映）**。実運用で発生した支払漏れの原因分析をもとに、4つの安全装置を標準化。
  `osi-finance-mf-sync` に **Step 0「口座連携の健全性チェック」**（口座ごとの最終取込日を見て3営業日途絶で警告。
  連携停止に気づかず台帳が腐る事故の起点を塞ぐ）と **Step 9「台帳への実績反映」**（銀行明細で確認できた支払だけを
  台帳の支払済へ自動更新。手更新をやめてステータスの鮮度を保つ）を追加。
  `osi-finance-payment-detect` に **送信元アドレスによる検出**（件名に「請求書」と書かない取引先を構造的に拾う）と
  **定期支払の未着検知**（当月分が未起票の毎月固定支払を警告）、**期日3営業日以内の未払い提示**を追加。
  `config/osi-finance-settings.example.md` に **「7-3. 定期支払先（毎月固定）」**（月額・サイクル・請求書メールの送信元・
  件名の特徴・支払方法）を新設。`monthly-rules.md` に判断辞書を大幅追記：源泉徴収の判定軸（外注/雇用ではなく
  「個人か法人か × 所得税法204条の列挙報酬か」・司法書士等の1万円控除・翌月10日納付）、
  カード会社への出金は保証金（振込）と利用代金決済（口座振替）の2種類、支払仕訳前の発生計上チェック（二重計上の最頻出パターン）、
  インボイス区分の期間統一と80%控除の計上方法、借入返済（元金＋利息／償還予定表が無い場合の暫定処理）、
  §12「支払漏れを防ぐ運用」（原因の連鎖と4つの止め方・推奨スケジュール）、§13「頻出する細かい科目」。

- v0.6.0：**AR の MF 計上**を追加し、MF を請求(AR)・支払(AP) 双方の単一会計正本化。
  `osi-finance-ar-sync`（請求済→売上計上((借)売掛金/(貸)売上+仮受消費税)・入金済→消込((借)普通預金/(貸)売掛金)を
  請求書IDで MF と突合・人レビューのうえ登録。`scripts/build_ar_journal.py`・`references/ar-journal-rules.md` 同梱）を新設。
  `osi-finance-monthly` に AR 突合ステップを追加、`osi-finance-invoice` に「発行と会計計上は分離」注記、
  `config/osi-finance-settings.example.md` に「AR 会計設定」（売上高/仮受消費税/売掛金/課税売上税区分/入金普通預金科目・
  入金bank連携の扱い）を追加。**`osi-finance-dashboard` を MF 試算表 PL/BS 正本に作り替え**（売上=正しい純損益、
  売掛金(未回収)=BS、画面を「①今月オペレーション／②履歴・分析」の2用途に分離。売上=0 でも落ちないガード）。
  会計ルール（科目・税区分・計上タイミング）は各社の税理士確認を前提。源泉・値引・貸倒は初期対象外。
- v0.5.0：Phase 2（品質・運用）。発火eval データセット（`docs/eval/firing-tests.yaml`）＋
  runner（`scripts/eval_skill_firing.py`、整合性検証＋skill-creator eval-set 生成）、
  `osi-finance-setup` の**導入完了レポート**（B表スモークの自動実行・`references/setup-completion-report.md`）、
  共通の**エラー処理ガイド**（`docs/エラー処理ガイド.md`、大容量PDF＝ローカル同期／添付＝Superhuman）を
  各スキル末尾から参照。QAチェックリストの Phase 2 残タスクを反映。
- v0.4.0：会計ダッシュボード（`osi-finance-dashboard`、ライブ・アーティファクト）を追加。
  `docs/QA-リリースチェックリスト.md`（配布前ゲート・導入スモーク・発火テスト・運用安全）を新設。
- v0.3.0：プロダクト化マイルストーン。`osi-backoffice` → **`osi-finance`** に独立化。
  `osi-finance-setup`（導入オーケストレータ）・`docs/導入ガイド.md`（顧客向け）を新設。
  スキル本文の組織固有プロバイダ名を役割名＋例示に統一。
