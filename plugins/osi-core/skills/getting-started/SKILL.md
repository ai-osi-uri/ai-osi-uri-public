---
name: getting-started
description: >
  スキルを入れた直後の「はじめて」を、会話 1 回で終わらせる入口スキル。会社プロファイル（osi-profile.md）を作り、
  案件フォルダ・台帳・ダミー案件のプロジェクトキットを書き出し、繋がっているコネクタを実際に叩いて
  「今使える／繋げば使える／鍵が要る」の表を返し、0 段目のスキルを 1 本動かして終わる。
  「はじめて」「セットアップして」「導入したい」「最初に何をすればいい」「環境を整えて」「プロファイルを作って」
  「何が使える？」「使えるようにして」「初期設定」で発動する。既に osi-profile.md がある環境では、
  設定の点検（コネクタの疎通・不足キーの補完）だけを行い、作り直さない。
# コネクタは「あれば実測する」任意の対象。依存ではないので requires には書かない
connector_prose_ok: [AI_OSI_URI_Deploy, AI_OSI_URI_Finance, ai-osi-uri-creative, slack, docusign, plaud, gmail, google-calendar, obsidian, money-forward, notion]
---

# はじめて（getting-started）

## §0 定義と不変条件

**導入が済んだ** とは、次の 3 点が揃い、0 段目のスキルが 1 本、実物の成果物を出した状態を指す。

1. **プロファイル** — 連結フォルダ直下に `osi-profile.md` があり、社名・案件フォルダ・台帳の場所・使うコネクタが埋まっている
2. **プロジェクトキット** — 案件フォルダの雛形・営業管理表・アプリ台帳・フォルダ規約（CLAUDE.md）があり、ダミー案件が 1 件入っている
3. **コネクタの実測** — 繋がっているコネクタを**実際に叩いて**判定した「今使える／繋げば使える／鍵が要る」の表がある

導出の型：**聞く（5 問まで）→ 作る（無いものだけ）→ 叩く（宣言でなく実測）→ 動かす（1 本）→ 表にして返す**。

不変条件（破ったら失敗）：

- 質問は **5 問まで**。答えられない項目は既定値で作り、「あとで `osi-profile.md` を直せばよい」と伝える。質問攻めにしない
- **機微値（インボイス登録番号・法人番号・口座）はチャットで聞かない。** 空欄で作り、本人がファイルに書く手順だけ示す
- **既にあるものは上書きしない。** `osi-profile.md` も台帳もフォルダも、無いものだけ作る（`init_kit.py` が保証する）
- コネクタは **宣言ではなく実測**。ツールが見えても叩いて失敗すれば「未接続」。叩けないものを「使える」と言わない
- 完了は **成果物が出たとき**。表を出して終わりにせず、0 段目のスキルを 1 本動かして「できた」まで持っていく

事故の型（過去に起きたこと・起きうること）：手順書が 30 ステップに膨らんで誰も最後まで行かない／会社固有の値をスキル本文に直書きして別会社で壊れる／`.mcpb` を入れたのに古いプロセスが残って「動かない」（新しいセッションで再試行）／Drive の同期フォルダが cloud-only で bash から見えず「無い」と誤報。

## Step 1. 現状を見る（質問の前に）

1. 連結フォルダを特定する（Cowork の連結フォルダ。複数あれば会社の資料が入っている方）。無ければ「Claude にフォルダを 1 つ連結してください（Drive の共有フォルダ推奨）」で止まる
2. `python3 scripts/init_kit.py --root <連結フォルダ> --status` で、`osi-profile.md` の有無と直下のフォルダ一覧を取る
3. 見えているツール名から、繋がっているコネクタを列挙する（AI OSI URI Deploy / AI OSI URI Finance / AI OSI URI Creative / Gmail / Slack / Google Drive / Calendar / MoneyForward / DocuSign / Plaud / Obsidian）。**どれも必須ではない**。このスキル自体はコネクタ無しで動く

`osi-profile.md` が**既にある**場合は Step 2〜3 を飛ばし、Step 4（実測）と Step 5（1 本動かす）だけ行う。不足キー（雛形にあってプロファイルに無いキー）があれば追記を提案する。

## Step 2. 5 問だけ聞く

`AskUserQuestion` で、まとめて聞く（1 問ずつ往復しない）。

| # | 質問 | 使い先 |
|---|---|---|
| 1 | 会社名（登記名）と代表者。あれば読みやすい表記（英語名など） | `company.name` `company.name_display` `company.representative` |
| 2 | 案件フォルダは**これから作る**か、**既にある**か（あるなら、案件フォルダの親と営業管理表の場所） | `layout` / `paths.projects` / `ledgers.sales` |
| 3 | 台帳の担当者列に使う自分の表記（例: 山田） | `members.owner` |
| 4 | 使うコネクタ（Step 1 で見えたものを候補に、使う／使わないを選ぶ） | `connectors.*` |
| 5 | 最初にやりたいこと 1 つ（例: 議事録／提案書／請求書／アプリ／動画） | Step 5 で動かすスキルの選定 |

住所・ドメイン・URL・事業内容は、会社名から Web 検索で補完できるなら**補完して確認だけ取る**（質問数に入れない）。
登記名の全角/半角・スペースは登記どおりに。分からなければ本人に登記簿の表記を確認してもらう。

## Step 3. 作る（無いものだけ）

回答を `answers.json` にまとめ、`python3 scripts/init_kit.py --root <連結フォルダ> --answers answers.json` を実行する。
（`--dry` で先に「何が作られるか」を見せてから実行してもよい。）

作られるもの（既にあるものは飛ばす）：

| もの | 場所（規定。`layout=existing` なら回答の場所） |
|---|---|
| 会社プロファイル | `osi-profile.md`（雛形 `assets/osi-profile.example.md` の `{{ }}` を回答で埋めたもの。機微値は空欄） |
| 案件フォルダの親と雛形 | `案件/` `案件/000.フォルダ構造テンプレート/{01_提案・見積, 02_契約, 03_制作・成果物, 04_請求, 05_受領資料}` |
| 営業管理表 | `営業管理/営業管理表.xlsx`（ヘッダ 5 行目・27 列・「マスタ」タブにステータス値） |
| 運用ルール・アプリ台帳 | `営業管理/営業管理表_運用ルール.md` `案件/_アプリ台帳.md` |
| フォルダ規約 | `CLAUDE.md`（このフォルダの読み方。案件フォルダの命名・台帳の場所） |
| ダミー案件 | `案件/122.サンプル株式会社/`（ヒアリングメモ入り）＋ 台帳に 1 行。**初回の動作確認に使い、要らなくなったら消す** |
| 経理フォルダ | `経理/`（中身は `osi-finance-setup` が作る。ここでは空フォルダだけ） |

実行後、`created` / `skipped` を人が読める形で報告する。**登録番号・口座は「`osi-profile.md` の該当行に直接書いてください」と案内する**（チャットに貼らせない）。

## Step 4. 叩く（コネクタの実測）

`connectors.*` が true のものと、Step 1 で見えたものを、**実際に 1 回ずつ叩く**。

| コネクタ | 叩くもの | 判定 |
|---|---|---|
| AI OSI URI Deploy | `health_check` | `github.valid` / `vercel.valid` が true → 使える。false → 鍵が要る（Settings → Extensions） |
| AI OSI URI Finance | `health_check` | 台帳フォルダが読めれば使える。MF は `mfi_connect_status` |
| AI OSI URI Creative | `list_models` | 返れば使える。鍵エラーなら BYOK 鍵が要る |
| Gmail / Calendar / Drive / Slack | 読み取り 1 回（ラベル一覧・カレンダー一覧・最近のファイル・チャンネル検索） | 返れば使える。ツール自体が無ければ「Connectors で接続」 |
| MoneyForward / DocuSign / Plaud / Obsidian | 軽い読み取り 1 回 | 同上 |

結果を `references/tiers.md` の段に重ねて、**この環境の表**を出す：

```
| 段 | 状態 | 使えるスキル（例） | 次の一手 |
| 0 段目 | 今使える | 提案書・事業計画・ファクトチェック・ペルソナ … 39 本 | — |
| 1 段目 | Gmail ○ / Calendar ○ / Slack ✕ | 議事録→台帳、空き時間取得 … | Slack を Connectors で接続 |
| 2 段目 | Deploy ✕（鍵未設定）/ Finance ○ / Creative ✕ | 請求書発行 … | Deploy 拡張に GitHub PAT と Vercel Token |
```

「次の一手」は、`docs/onboarding.md` の該当節（§3 `.mcpb`・§4 Connectors・§6 Deploy）を **1 行で**指す。手順を本文に写さない。

## Step 5. 動かす（1 本）

Step 2 の質問 5 の答えに合わせて、**今使える段**のスキルを 1 本、ダミー案件で動かす。

| やりたいこと | 動かすスキル | 入力 |
|---|---|---|
| 提案書・営業 | `osi-docs:storyline-gate` → `osi-sales:proposal-package` | `案件/122.サンプル株式会社/05_受領資料/初回ヒアリングメモ.md` |
| 議事録 | `osi-sales:meeting-minutes`（1 段目。無理なら `osi-core:transcript-router` に短い文字起こしを貼る） | ダミーの文字起こし 10 行 |
| 請求書・経理 | `osi-finance:osi-finance-setup` に引き継ぐ（経理は専用の初期設定がある） | — |
| アプリ | `osi-deploy:create-app`（2 段目。Deploy が ✕ なら `osi-deploy:local-project-output`） | 「サンプル株式会社の FAQ ページ」 |
| 動画 | `osi-creative:ai-video-production`（2 段目。Creative が ✕ なら見送り、次の一手を案内） | — |
| 決めていない | `osi-marketing:persona-design` | ヒアリングメモ |

成果物が出たら、それを案件フォルダの `03_制作・成果物/` に置き、**「できたこと 1 行」と「次に繋ぐと広がること 1 行」**で締める。

## 出力（最後に返すもの）

1. 作ったもの／飛ばしたもの（パス）
2. この環境の段の表（Step 4）
3. 動かしたスキルと成果物の場所（Step 5）
4. 本人がやる残り（あれば）：機微値の記入、Connectors の接続、`.mcpb` の鍵。**各 1 行・リンク付き**

所要の目安は 15 分。超えそうなら Step 5 を「次回」に回し、1〜4 で一度返す。
