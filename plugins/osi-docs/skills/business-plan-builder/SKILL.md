---
name: business-plan-builder
description: 新規事業・新会社の「事業計画一式」を作る唯一の窓口（オーケストレータ）。会議メモ・構想・既存資料を起点に、要件をヒアリングして「何を作るか」を判断し、(1) 財務3表フル連動モデル(Excel)、(2) 投資家向けピッチ(pptx)、(3) 文章の事業計画書(md/docx)、(4) 社内報告メッセージ下書き、を必要なものだけ生成・整合させる。財務数値は (1) の月次連動モデルを正本とし、pptx・docx・報告はその数値に一致させる。立ち上がりはコンサバに引き、「いくら資金が必要か（資金トラフ）」まで出す。「事業計画を作って」「○○の新規事業の計画一式」「事業計画書とピッチと財務モデル」「資金調達用の事業計画」「会議メモから事業計画に」「新会社の数値計画と提案資料」などで発動する。財務モデル単体は atomic `financial-model-3statement`、pptx 整形は `pptx-custom`、Word は `docx` を内部で使う。※ 既存リード向けの営業提案書は osi-sales 側（proposal-package 等）の担当。本スキルは自社/新規事業の事業計画づくり専用。
version: 0.1.0
---

# 事業計画ビルダー（オーケストレータ）

新規事業・新会社の**事業計画一式**を、要件に応じて過不足なく作る単一窓口。各成果物を別々に作るのではなく、**財務モデルを正本に全成果物の数値・ストーリーを一致**させるのが役割。

## 作れる成果物（要件次第で取捨選択）

| # | 成果物 | 担当 | 使う場面 |
|---|--------|------|----------|
| A | 財務3表フル連動モデル（Excel） | atomic `financial-model-3statement` | ほぼ必須。数値・資金需要の正本 |
| B | 投資家向けピッチ（pptx, 15枚目安） | `pptx-custom` | 出資・社内合意を取りに行くとき |
| C | 文章の事業計画書（md / docx） | `docx`（必要なら）/ md直書き | きちんと読ませる正本が要るとき |
| D | 社内報告メッセージ（下書き） | 本スキルが直接生成 | 上長・共同創業者へ報告するとき |

## オーケストレーションの流れ

### Step 0. インプット把握
渡された素材（会議文字起こし・構想メモ・既存xlsx/pptx・参照フォーマット）を読む。何が既にあり、何が欲しいかを掴む。

### Step 1. 要件ヒアリング（AskUserQuestion, 1度に4問まで）
最低限これらを確認：
- **対象事業／会社**：何の事業か、新会社か既存社か、第一弾は何か、舞台
- **作る成果物**：A〜D のどれが要るか（複数選択。既定は A＋B、必要に応じ C・D）
- **想定読者**：投資家中心か、社内実行向けか、両方か
- **立ち上がりの強気/弱気**：既定はコンサバ（採用→稼働ラグ×稼働率）
- **資金の方針**：必要額ベースで出すか、目標調達額ありか
不明点が無ければ既定で進めてよい。

### Step 2. 財務モデル（A）を作る ← 数値の正本
`financial-model-3statement` を使う（SKILL を読む）。**先に raise=0 で必要資金（資金トラフ）を確認 → 必要額＋バッファを増資に設定 → 再生成**。`verify.py` が `RESULT: OK`（Balance Check / 連動Check が全月0）になるまで完了としない。
ここで確定する正本数値：FY別 売上/売上総利益/営業利益/利益率/当期純利益、必要資金（トラフ）、推奨調達額、調達後最低現金、（出口を語るなら）EXIT営業利益×倍率＝EV と投資家リターン。

### Step 3. ピッチ（B）を作る ← 数値はAに一致
`pptx-custom` の作法で15枚目安。財務・資金・出口スライドは **Step 2 の正本数値をそのまま使う**。構成の型（流用可）：
1 表紙 / 2 サマリー / 3 大きな考え方 / 4 課題 / 5 ソリューション / 6 なぜ勝てる/潰れない / 7 第一弾(GTM) / 8 ビジネスモデル(ユニットエコノミクス) / 9 市場 / 10 横展開 / 11 競合 / 12 財務(表＋グラフ) / 13 資金(必要資金・使途) / 14 出口 / 15 直近アクション・体制。
レンダリング→画像で視覚QA（重なり/折り返し/見切れ）まで行う。

### Step 4. 文章の事業計画書（C, 任意）
md で章立て（エグゼクティブサマリー〜出口〜リスク）。docx が要るなら `docx` スキルで清書。数値は A に一致。

### Step 5. 報告メッセージ（D, 任意）
上長・共同創業者向けに、要点（作ったもの・コンサバ前提・必要資金・5カ年・相談したい論点）を簡潔にまとめた下書きを出す。社内チャット向け短文 / メール / そのまま貼れる形のいずれかを選べるようにする。

### Step 6. 整合チェックと格納
- pptx・docx・報告の数値が A と一致しているか最終確認（特に売上/営業利益/必要資金/調達額/EV/リターン）。
- 案件成果物は**このリポジトリに保存しない**。outputs に作ってユーザーに提示し、案件の Drive フォルダ（`21.PJT資料/...` 等）へ格納。`present_files` で提示する。

## 判断のガイド
- 「とりあえず数字を見たい」→ A だけ。
- 「出資・合意を取りに行く」→ A＋B（＋必要ならD報告）。
- 「きちんと読ませる正本が要る」→ A＋C、必要ならB。
- 立ち上がりは特に指定がなければ**コンサバ**で引く（楽観値は意思決定を誤らせる）。
- 「1〜2年でEXIT」等の希望と、数値モデルの出口時期がズレる場合は**正直に指摘**し、出口時期か評価額のどちらを動かすか確認する。

## 関連スキル
- 核：`financial-model-3statement`（A）
- 整形：`pptx-custom`（B）、`docx`（C）
- 営業の提案書（既存リード向け）は別系統：osi-sales の `proposal-package` 等。
