// PLACEHOLDER FILE.
//
// The real google-services.json is generated by `mobile-firebase-setup`
// (or downloaded from Firebase Console) and stored ONLY on the developer
// machine + as a GitHub Actions secret (`GOOGLE_SERVICES_JSON_DEV_B64`).
//
// Do NOT commit real Firebase credentials to git. See .gitignore.
//
// In CI, the workflow decodes the secret into
// `apps/android/app/src/dev/google-services.json` (or `google-services.json`
// at the app module root) before `./gradlew bundleRelease`.
{
  "project_info": {
    "project_id": "REPLACE_WITH_REAL_FIREBASE_PROJECT_ID"
  }
}
