---
name: meeting-minutes
description: |
  商談議事録を Plaud の文字起こしから自動生成し、Drive の `03_制作・成果物/` に docx として格納したうえで、営業管理表（`{{ledgers.sales}}` の `取引先管理` タブ）の最終接触日・最終議事録日・メモ・ステータスを更新する一気通貫スキル。「議事録にして」「○○社の議事録」「案件ID 098 の議事録」「セーフティ&ベルとの今日の打ち合わせを議事録化」「Plaud の文字起こしを議事録に」「商談メモを構造化して」など、商談・打ち合わせの議事録作成に関わるリクエストで発動する。日本語の営業ワークフローに特化。一般的な社内ミーティング・全社会議の議事録には使わないこと（このスキルは案件単位の営業管理表と紐付く）。
requires_connectors:
  - server: slack
    provision: user-install
  - server: box
    provision: user-install
  - server: plaud
    provision: user-install

---

# 商談議事録自動化スキル

> **組織固有値はプロファイルから読む。** 本文の `{{paths.*}}` `{{ledgers.*}}` `{{company.*}}` `{{members.*}}` は、連結フォルダ直下の `osi-profile.md`（雛形: `config/osi-profile.example.md`）の値に置き換えて解釈する。無ければ会社名・案件フォルダ・台帳の有無・使うコネクタを質問して先に作る。値をここに直書きしない。

自社の営業ワークフローにおいて、Plaud で録音した商談の文字起こしを
構造化された議事録に変換し、**議事録の正本を Drive の docx として格納**したうえで、
案件マスタ（営業管理表）の状態だけを更新するスキル。

## なぜこのスキルが必要か

AS-IS では、Plaud で録音した文字起こしを主担当が手で読み込み、

1. サマリを書き起こす
2. 次アクションを抽出する
3. 重要な発言をピックアップして memo
4. docx に整形して Drive にアップロード
5. 台帳のメモ欄に次アクションを1行書く
6. 案件ステータス・最終接触日を必要に応じて更新

をやっている。これが商談 1 件あたり 30 分〜1 時間。
このスキルなら「今日の○○社の録音を議事録にして」の一言で、Plaud MCP から
文字起こしを直接取得して全自動化する（貼り付けも従来どおり可）。

---

## 前提：案件マスタは営業管理表

案件情報の正本は **`{{ledgers.sales}}`**（タブ `取引先管理`、ヘッダは5行目）。
**ディレクトリ経由（openpyxl）で読み書きする。Google Sheets API・GCPサービスアカウントは使わない。**
台帳の触り方・不変条件・列の一覧は
**`../new-lead-registration/references/ledger-access.md` を必ず読むこと**（osi-sales 共通の正本）。

```bash
L=../new-lead-registration/scripts/ledger.py
python3 $L columns                       # 列名・ステータス値・パス
python3 $L find --company "{企業名}"      # 表記ゆれを吸収して行を引く
python3 $L find --case-id {案件ID3桁}
```
シートID・タブ名などの組織固有値は `config/osi-sales-settings.md` を参照してください。

- ヘッダは5行目、データは6行目以降
- 主な列: A=取引先 / B=URL / C=メモ（顧客課題・アクション）/ D=主担当 / W=ステータス / X=想定額 /
  Y=獲得日 / **Z=案件ID** / **AA=最終接触日** / **AB=最終議事録日**
- **セクション見出しは無い**（1案件1行・行は動かさない）。旧gsheetにあった次の区分は、いまはステータス列が表す:
  `既存取引先（契約済み・稼働中）` / `新規契約済み（受注確定・請求未開始）` /
  `案件化（提案中・商談）` / `リード（未提案・新規アプローチ）` / `失注（参考）`
- ステータス値は `リード` / `商談中` / `金額提示済み` / `契約済み` / `デリバリー中` / `失注` の6つだけ。
  `KPIダッシュボード` タブが集計しているので**値を勝手に増やさない**
- 列は**ヘッダ行の文字列**で指定する（列レターは使わない）。実際の列名は
  `ledger.py columns` の戻り値 `columns` で確認できる（存在しない列を指定するとエラーで止まる）
- 月別売上（E〜P）と売上合計（Q）は数式列なので書き込まない

**議事録の本文は台帳に持たない。** 議事録の正本は Drive の
`{{paths.projects}}/{{paths.project_naming}}/03_制作・成果物/` に置いた docx で、
台帳（営業管理表）は「状態」（最終接触日・最終議事録日・次アクション1行・ステータス）だけを持ちます。

---

## 重要：このスキルの責任範囲

Claude（あなた）が **「文字起こしの構造化」** を担当する。
**docx の整形は `scripts/build_minutes.py` に完全に任せる**。

つまり Claude はワード文書生成コードを書かない。代わりに、

- Plaud MCP から取得した（または貼り付けられた）文字起こしを読み、
- サマリ・次アクション・重要発言を抽出して JSON にし、
- スクリプトで docx を生成し、
- Drive に格納して営業管理表の状態を更新する。

---

## ワークフロー

```
1. 案件特定（営業管理表を検索 — proposal-package と同じロジック）
2. 文字起こしを取得（Plaud MCP で直接取得が基本／貼り付け・Gmail転送・Drive はフォールバック）
3. テキストを構造化（サマリ / 次アクション / 重要発言 / ステータス示唆）
4. ユーザーに構造化結果を確認（必要に応じて修正）
5. docx 生成（scripts/build_minutes.py）
6. Drive にアップロード（03_制作・成果物/ 配下）
7. 営業管理表の `顧客課題メモ` 列に議事録1行を追記
7.5 `最終接触日` 列／`最終議事録日`（※現在の台帳には存在しない）を自動更新
8. `ステータス` 列更新の提案（必要なら）
9. 結果報告
```

---

## Step 1: 案件特定

ユーザーの発話から対象案件を特定する：
- 案件ID（「案件ID 098」「98 番」）
- 企業名（「セーフティ&ベル」「サンプル社」）

営業管理表を検索して該当行を特定する：

```
python3 $L find --company "{企業名}"    # 表記ゆれ（株式会社・促音・記号）は吸収される
python3 $L find --case-id 098           # 案件IDが分かっているならこちらが確実
```

- 候補が1件 → そのまま採用（戻り値の `row` / `案件ID` を保持）
- 候補が複数 → `AskUserQuestion` でユーザに選ばせる（1社で複数案件が走ることがあるため）
- 候補が0件 → 「営業管理表に該当案件がありません。先に `new-lead-registration` を実行してください」と案内（議事録単独で完結はしない）

続けて `ledger.py read` で対象行の全項目を取得し、**`顧客課題メモ` 列の既存文言**と
**`最終接触日` 列／`最終議事録日` 列（※現在の台帳には存在しない）の現在値**を保持しておく。
Step 7・7.5 でこれらを使う（既存メモの追記、日付の巻き戻し防止）。

以降の更新は必ず**案件ID（`case_id`）で行を特定**すること（同名・複数案件で誤爆しないため）。

---

## Step 2: 文字起こしの取得

### 2-A: Plaud MCP で直接取得（プライマリ）

Plaud MCP コネクタ（ツール名に `plaud` を含む `list_files` / `get_file` /
`get_transcript` / `get_note` / `login` 等）が利用可能なら、貼り付けを待たずに直接取りに行く。

1. **録音の特定**：`list_files` を使う。
   - キーワード（`query`）：企業名・録音名の一部（大文字小文字不問）
   - 日付（`date_from` / `date_to`、YYYY-MM-DD）：「今日の」「昨日の」「5/6 の」を変換
   - 候補が複数 → `AskUserQuestion` で選ばせる（録音名・開始時刻・長さを添える）
   - 候補が 0 件 → 検索条件を緩めて再検索し、それでも無ければ 2-B へフォールバック
2. **文字起こしの取得**：`get_transcript` で全文（タイムスタンプ・話者ラベル付き）を取得。
   話者ラベルがあるので、貼り付けより話者推定の精度が上がる。
3. `get_note`（Plaud の AI 要約）は **参考情報に留める**。サマリ・次アクションの構造化は
   Step 3 で Claude 自身が全文から行う（Plaud 要約の転記で済ませない）。
4. **認証エラー（401 / Not authenticated）** → `login` ツールで再サインインを促してから再試行。

### 2-B: フォールバック（MCP が無い／録音が見つからない場合）

優先順：
1. **チャットへの貼り付け**：従来どおり Plaud アプリからのコピペを受け付ける
2. **Gmail 転送メール**：Plaud から Gmail に自動転送されている場合、Gmail 検索
   （`search_threads` で `from:plaud` や録音名・日付）で本文の文字起こしを取得する
3. **Drive ファイル**：ファイル名指定があれば `read_file_content` で取得

どれも無ければ `AskUserQuestion` で「Plaud の文字起こしを貼り付けてください
（または録音名・日付を教えてください）」と促す。

> 取り込み後の特徴対処・話者推定の詳細は `references/transcript-parsing.md` を参照
> （session-review / shodan-prep と共通の作法）。

---

## Step 3: テキスト構造化

文字起こしを以下の 4 セクションに整理する。**このステップが Claude の主要責任**。

### サマリ（必須・3-5 点）

「議事録を読まない人でも、3 点読めば何が起きたか分かる」レベルに圧縮。
- 相手の最大の関心事
- 決まったこと / 決まらなかったこと
- 次のステップ

時系列ではなく **重要度順**。

### 次アクション（必須）

「誰が・いつまでに・何を」を明示。曖昧なものは省く。
JSON では：

```json
{
  "todo": "見積もり提案書（yourrecord + CAIO Advanced）を作成",
  "due": "2026-05-13",
  "owner": "自社担当"
}
```

期限は YYYY-MM-DD 形式が読み取れる場合のみ。曖昧な場合は「次回打ち合わせまで」等の文字列でも OK。

担当が「弊社」「先方」と曖昧な場合は、文字起こしから読み取れる人名を優先。読み取れなければ会社名の方を入れる。

### 相手の重要発言（必須）

「あとで提案書に使えるキーワード」を中心に、原文に忠実に引用。
- 課題の核心を表す発言
- 意思決定の根拠になる発言
- 価格や期間に関する具体的な発言

3-5 件が目安。多すぎると埋もれる。

### 案件ステータスへの示唆（任意・あれば追加）

商談の結果を踏まえて、営業管理表の `ステータス` 列を変えた方が良い場合の提案。
Claude が自律的に判断してよい。

ステータスは `リード` / `商談中` / `提案中` / `金額提示済み` / `受注` / `契約済み` / `デリバリー中` / `保留` / `失注` / `終了` の10値のみ。

例：
- 「見積もりに進む合意があった」→ `商談中` → `金額提示済み`
- 「発注の意思表示があった」→ `金額提示済み` → `契約済み`
- 「他社に決まった」→ `失注`

これは **Step 8 で営業管理表の更新を提案するか判断する材料** にも使う。

---

## Step 4: ユーザー確認

構造化した JSON を簡潔にチャットに表示し、ユーザーに「この内容で議事録化しますか？」と確認。
修正点があれば反映してから次へ。

---

## Step 5: docx 生成

```bash
python scripts/build_minutes.py <minutes.json> <output.docx>
```

出力パス：`/tmp/minutes_<ID3桁>_<企業名>_<YYYYMMDD>.docx`

---

## Step 6: Drive にアップロード

格納先：
```
Drive/{{paths.projects}}/{{paths.project_naming}}/03_制作・成果物/
```

ファイル名：`{YYYYMMDD}_議事録_{企業名}.docx`

例：`20260506_議事録_セーフティ&ベル.docx`

`03_制作・成果物/` フォルダが存在しない場合は作成する。

---

## Step 7: 営業管理表の `顧客課題メモ` 列に1行追記

**議事録の正本は Drive の docx**（Step 6）であり、本文を台帳に写すことはしない。
営業管理表には「この案件が次に何をするか」が一目で分かる**1行だけ**を残す。

`ledger.py update`(case_id="{案件ID3桁}") で `顧客課題メモ` 列を更新する。書式：

```
{YYYY-MM-DD} 議事録: {次アクションの最重要1件}
```

例：`2026-07-21 議事録: 見積もり提案書（yourrecord + CAIO Advanced）を自社担当が7/24までに作成`

```bash
L=../new-lead-registration/scripts/ledger.py
python3 $L update --case-id 098 \
  --append-memo "{YYYY-MM-DD} 議事録: {次アクション最重要1件}"
```

- **既存メモは消さない。** `--append-memo` は上書きではなく末尾への追記なので、自分で連結しない
- 追記するのは1行だけ。サマリ・重要発言の転記はしない（それは docx にある）
- 次アクションが1件も抽出できなかった場合は `{YYYY-MM-DD} 議事録作成` とだけ書く
- 案件IDが重複している行（`009` `030` `092`）は `--case-id` で一意に引けない。その場合は `--row` で行番号を指定する

---

## Step 7.5: 最終接触日 / 最終議事録日の自動更新

議事録を記録したら、営業管理表の日付列を **自動で** 前進させる。これは「議事録の鮮度」と
「解約予兆（放置案件）」を一目で見えるようにするための土台で、ここを人手に委ねると必ず更新漏れが
起きる。だから Step 8 のステータス変更（要確認）とは違い、**確認は取らずに自動で行う**。

更新する値（いずれも議事録の日付 = `meta.date`）：
- **`最終接触日`** ← `meta.date`（商談した＝接触したため）
- ※ `最終議事録日` 列は現在の台帳に存在しない。議事録を作った事実は `顧客課題メモ` への追記で残す

```bash
python3 $L update --case-id 098 --data '{"最終接触日": "{meta.date}"}'
```

> **`最終議事録日` 列は現在の台帳に存在しない。** `ledger.py columns` で確認すること。
> 議事録を作った事実は `--append-memo` で顧客課題メモに残す。

Step 7 と同じ行への更新なので、`--data` と `--append-memo` をまとめて1コールにしてよい。

**日付を巻き戻さないこと（重要）**。過去の商談を後から議事録化する場合、`meta.date` が
既存の `最終接触日` の値より **古い** なら、その列は `updates` に含めない（新しい場合のみ前進させる）。
Step 1 で `ledger.py read` から取得した現在値と比較して判断する。空なら無条件でセットする。

> 列名は営業管理表のヘッダ文言（`最終接触日` / `最終議事録日`）で指定する。
> 存在しない列のエラー が返ったら、`ledger.py read` の `columns` で実際のヘッダ文言を確認すること。

---

## Step 8: 案件ステータス更新の提案

Step 3 で抽出した「ステータス示唆」が空でなければ、`AskUserQuestion` で
「営業管理表のステータスを更新しますか？」と確認：

```
- `ステータス` 列: 商談中 → 金額提示済み
```

「はい」なら `ledger.py update`(case_id=..., updates={"ステータス": "{新しい値}"}) で更新。
「いいえ」ならスキップ。

- ステータスは10値（`ledger.py columns` で確認できる）以外にしない
- **進んだ方向にだけ動かし、勝手に巻き戻さない**（`失注` は明確な発言がある場合のみ）
- **行は動かさない。** ステータス列を変えるだけでよい（セクションという概念は無い）

---

## Step 8.5: Slack 通知（slack-notify が利用可能な場合）

`slack-notify` スキルをサブスキルとして呼び出し、議事録公開を
**案件チャンネル**（必須）に通知する。

```
slack-notify(
  event_type="minutes_published",
  payload={
    "company": "{企業名}",
    "id": "{ID3桁}",
    "minutes_url": "{Drive ファイル URL}",
    "next_action": "{次アクションの最重要1件}",
    "next_action_due": "{期限YYYY-MM-DD}",
    "owner": "{担当}",
    "project_channel_id": "{案件CHのID（分かれば）}"
  },
  mention="@here"
)
```

`project_channel_id` が取得できない場合は slack-notify 側のフォールバック
ルール（`#sales` に集約）に従う。slack-notify 自体が未整備の場合はスキップ。

## Step 9: 結果報告

ユーザーに以下を簡潔に返す：

```
✓ {企業名}（案件ID {案件ID3桁}）の議事録が完了しました

- docx: <Drive ファイル URL>
- Drive フォルダ: {{paths.projects}}/{{paths.project_naming}}/03_制作・成果物/
- 営業管理表: {行番号}行目
  - 最終接触日 / 最終議事録日 = {meta.date}（自動更新）
  - `顧客課題メモ` 列に「{YYYY-MM-DD} 議事録: {次アクション}」を追記
  - ステータス: {旧} → {新}（実施済 / スキップ）
- Slack 通知: (案件CH に投稿済 / スキップ)
- 次アクション {N} 件を抽出（うち主担当担当 {M} 件）
```

---

## 構造化のコツ

### Plaud の文字起こしの特徴と対処

> 文字起こしの取り込み・特徴対処・話者推定は、**`references/transcript-parsing.md`**
> （meeting-minutes / session-review / shodan-prep 共通の作法・正本）に従う。

要点（詳細は上記 reference）：句読点が少ない（意味の塊で読む）／発言者ラベルが曖昧
（meta.attendees と文脈から人名推定）／数字・固有名詞は怪しい（「(原文ママ)」注記、数字は
勝手に変えない）／長い独白は 1 行に圧縮。

### サマリの粒度の判断

- **良いサマリ**: 「先方の最大の課題は属人化。ベテランの定年退職が 2027 年に集中することが判明」
- **悪いサマリ**: 「営業の話をした」「DX について議論した」（具体性ゼロ）
- **悪いサマリ**: 「13:00-13:15 で挨拶、13:15-13:30 で課題ヒアリング、…」（時系列の引き写し）

### 重要発言の選び方

- 「これがあれば次の提案書が書ける」と思える発言を選ぶ
- 数字や固有名詞が含まれる発言は採用優先度高
- 「うちは○○なんですよね」のような自社観を表す発言は採用

### 案件ステータスの示唆を出す判断

- 出した方が良い: 商談の結果、明らかに次フェーズに進める／止める判断材料が出た時
- 出さない方が良い: 単なる近況交換、技術的な意見交換だけだった時

---

## やってはいけないこと

- スクリプト本体（`build_minutes.py`）の編集 — フォーマットは config 側で制御
- 議事録の本文（サマリ・重要発言・文字起こし）を営業管理表に写す — 正本は Drive の docx。台帳には `顧客課題メモ` の1行だけ
- `顧客課題メモ` 列を上書きする — 既存文言は残し、必ず**追記**する
- 数字を勝手に変更する — 怪しければ「(原文ママ)」と注記する
- 案件`ステータス` 列を Claude が独断で更新する — 必ず `AskUserQuestion` で確認
- ステータスに10値以外を入れる — `KPIダッシュボード` タブが集計しているので値を増やさない
- 最終議事録日 / 最終接触日の更新で日付を巻き戻す — 既存値より新しい時のみ前進させる（過去分の遡及入力で鮮度を壊さない）。なお日付更新そのものは自動でよい（ステータスと違い確認不要）
- 営業管理表に対象案件がない時に、勝手に行を追加してから議事録化する — 本スキルの責務外。先に `new-lead-registration` を呼ばせる
- 数式列（月別売上 E〜P / 売上合計 Q）に書き込む

---

## エラーハンドリング

- **案件が見つからない（`ledger.py find` が0件）** → 「営業管理表に該当案件がありません。先に `new-lead-registration` を実行してください」と案内して終了
- **`ledger.py find` が複数ヒット** → `AskUserQuestion` で選ばせる。以降の更新は必ず `case_id` で行う
- **`ledger.py update` が「複数行が該当」で失敗** → `company` ではなく `case_id` を指定して再実行
- **`案件ID` 列が空** → Drive の `{{paths.projects}}/` からフォルダを探して補完し、`案件ID` 列に書き戻す
- **存在しない列のエラー が返る** → `ledger.py read` の `columns` で実際のヘッダ文言を確認して指定し直す
- **Plaud MCP が 401 / 未認証** → `login` ツールでサインインを促して再試行。直らなければ 2-B へフォールバック
- **Plaud に該当録音が無い** → 条件を緩めて再検索 → 無ければ貼り付け / Gmail / Drive へフォールバック
- **文字起こしテキストが空** → AskUserQuestion で再取得を促す
- **Drive にフォルダが無い** → その場で作成
- **既に同日の議事録がある** → 「上書きしますか？」と確認

---

## 参照ドキュメント

- `references/transcript-parsing.md` — 【共通・正本】Plaud 文字起こしの取得（MCP/フォールバック）・特徴対処・話者推定（session-review / shodan-prep と共有）
- `references/data-schema.md` — minutes.json の完全スキーマ
- `assets/example-minutes.json` — サンプル minutes.json（雛形として複製）

---

## トリガ例（再掲）

- 「議事録にして」
- 「○○社の議事録を作って」
- 「案件ID 098 の議事録」
- 「セーフティ&ベルとの今日の打ち合わせを議事録化」
- 「今日の○○社の録音を議事録にして」（← Plaud MCP で直接取得）
- 「Plaud の文字起こしを議事録にして」
- 「商談メモを構造化して Drive と営業管理表に入れて」

似ているが別スキルで対応する発話：

- 「会話を Obsidian に保存」 → `obsidian-knowledge-capture`
- 「全社会議の議事録」 → 通常の docx 生成（このスキルは案件単位）
